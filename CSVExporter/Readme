
Keep all the files in same folder

Create a file you wnat to export the CSV files into in the same folder. Name it Output Folder.

Then run the following commands in Command Prompt/Command Line Interface

javac BasicOSMParser.java

java BasicOSMParser map.osm Output Folder
